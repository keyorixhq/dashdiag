dist/
*.test
coverage.out
coverage.html
.DS_Store
prompts/
.claude/

# Generated `dsd` report output (e.g. dsd-report-<host>-<ts>.md) and report dirs
dsd-report-*.md
reports/

# Stray cross-compiled binaries built to the repo root (release assets live in
# dist/, ignored above). Exact, root-anchored names only — must NOT shadow the
# legit dist/dsd-linux-amd64 / dsd-linux-arm64 release assets.
/dsd-arm64
/dsd-linux

# Strategic documents — keep local until ENISA cofounder checks complete
KEYORIX_FOUNDATION.md
POSITIONING.md
STRATEGY.md
FEATURES.md
LAUNCH_PREP.md
PRODUCT_IDEAS.md

# Content drafts — release coordinated with launch
CONTENT_*.md
dsd
deploy.sh
.dsd/oval/
marketing-assets/**/*.oval.xml.bz2
marketing-assets/**/baselines/

# Local AI/dev context — not tracked in the public repo
CLAUDE.md

# transient binary built by scripts/check-replay-fidelity.sh
.dsd-fidelity.*

# throwaway build artifacts + local test binaries/captures (CLAUDE.md convention)
.scratch/
