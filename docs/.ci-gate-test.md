# CI gate verification

Temporary file to verify the docs-only diff-gate skip path still posts correctly-named required check-runs after the ci.yml matrix-skip fix (#914). Safe to ignore; this PR is not meant to be merged.
